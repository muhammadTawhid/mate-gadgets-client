import React from 'react';
import Header from '../Header/Header';

const Orders = () => {
    return (
        <div>
            <Header/>
            <h2>this is Orders</h2>
        </div>
    );
};

export default Orders;